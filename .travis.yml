language: cpp
compiler: gcc
dist: trusty

before_install:
  - git submodule init
  - git submodule update
  - ./configure.sh

script:
  - ./test.sh

notifications:
  email:
    on_success: never
    on_failure: never