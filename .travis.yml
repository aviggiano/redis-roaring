language: cpp
compiler: gcc
dist: trusty

before_install:
  - git submodule init
  - git submodule update
  - ./configure.sh

script:
  - ./test.sh
  - ./test_valgrind.sh

notifications:
  email:
    on_success: never
    on_failure: never