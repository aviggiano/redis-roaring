#!/usr/bin/env bash

git submodule init
git submodule update
cd deps/CRoaring/

# https://github.com/RoaringBitmap/CRoaring#building-with-cmake-linux-and-macos-visual-studio-users-should-see-below
mkdir -p build
cd build
cmake ..
make -DBUILD_STATIC=ON

